import 'package:flutter/material.dart';

class HomepageDetailScreen extends StatelessWidget {
  const HomepageDetailScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: TextButton(
          child: const Text('home page detail '),
          onPressed: () {},
        ),
      ),
    );
  }
}
